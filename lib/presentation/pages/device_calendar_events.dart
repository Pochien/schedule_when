import 'package:flutter/material.dart';
import 'package:schedule_when/presentation/screens/dcalendar_events_view.dart';

class DeviceCalendarEventsPage extends StatelessWidget {
  const DeviceCalendarEventsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const DeviceCalendarEventsView();
  }
}
