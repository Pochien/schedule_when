import 'package:flutter/material.dart';
import 'package:schedule_when/presentation/screens/dcalendar_view.dart';

class DeviceCalendarPage extends StatelessWidget {
  const DeviceCalendarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const DeviceCalendarView();
  }
}
