import 'package:flutter/material.dart';

/// 目前未使用, 直接寫在page
class DeviceCalendarEventsView extends StatefulWidget {
  const DeviceCalendarEventsView({Key? key}) : super(key: key);

  @override
  _DeviceCalendarEventsViewState createState() =>
      _DeviceCalendarEventsViewState();
}

class _DeviceCalendarEventsViewState extends State<DeviceCalendarEventsView> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
